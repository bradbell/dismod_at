.. _weight_table-name:

!!!!!!!!!!!!
weight_table
!!!!!!!!!!!!

xrst input file: ``xrst/table/weight_table.xrst``

.. meta::
   :keywords: weight_table, the, weight, table

.. index:: weight_table, the, weight, table

.. _weight_table-title:

The Weight Table
################

.. meta::
   :keywords: weight_id

.. index:: weight_id

.. _weight_table@weight_id:

weight_id
*********
This column has type ``integer`` and is the primary key for
the this table.
Its initial value is zero, and it increments by one for each row.

.. meta::
   :keywords: weight_name

.. index:: weight_name

.. _weight_table@weight_name:

weight_name
***********
This column has type ``text`` and has a different value for every row;
i.e., the names are unique and can act as substitutes for the primary key.
The names are intended to be easier for a human to remember than the ids.

.. meta::
   :keywords: n_age

.. index:: n_age

.. _weight_table@n_age:

n_age
*****
This column has type ``integer`` and is the number of age values
in this smoothing grid. It must be greater than zero.

.. meta::
   :keywords: n_time

.. index:: n_time

.. _weight_table@n_time:

n_time
******
This column has type ``integer`` and is the number of _time values
in this smoothing grid. It must be greater than zero.

.. meta::
   :keywords: example

.. index:: example

.. _weight_table@Example:

Example
*******
The file :ref:`weight_grid_table.py-name`
contains an example ``weight`` table.
