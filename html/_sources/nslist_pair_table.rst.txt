.. _nslist_pair_table-name:

!!!!!!!!!!!!!!!!!
nslist_pair_table
!!!!!!!!!!!!!!!!!

xrst input file: ``xrst/table/nslist_pair_table.xrst``

.. meta::
   :keywords: nslist_pair_table, lists, of, node, smoothing, pairs

.. index:: nslist_pair_table, lists, of, node, smoothing, pairs

.. _nslist_pair_table-title:

Lists of Node Smoothing Pairs
#############################

.. meta::
   :keywords: discussion

.. index:: discussion

.. _nslist_pair_table@Discussion:

Discussion
**********
This table can be used to specify a lists of nodes with
corresponding smoothings.

.. meta::
   :keywords: nslist_pair_id

.. index:: nslist_pair_id

.. _nslist_pair_table@nslist_pair_id:

nslist_pair_id
**************
This column has type ``integer`` and is the primary key for
the ``nslist_pair`` table.
Its initial value is zero, and it increments by one for each row.

.. meta::
   :keywords: nslist_id

.. index:: nslist_id

.. _nslist_pair_table@nslist_id:

nslist_id
*********
This column has type integer and is used to identify an individual lists
in this table.
This column has type ``integer`` and is the primary key for
the :ref:`nslist_table-name` .
Each *nslist_id* value corresponds to a different list of
( *node_id* , *smooth_id* ) pairs.

.. meta::
   :keywords: node_id

.. index:: node_id

.. _nslist_pair_table@node_id:

node_id
*******
This column has type ``integer`` and is a
:ref:`node_table@node_id` in the ``node`` table.
It specifies which node this smoothing should be applied to.

.. meta::
   :keywords: smooth_id

.. index:: smooth_id

.. _nslist_pair_table@smooth_id:

smooth_id
*********
This column has type ``integer`` and is a
:ref:`smooth_table@smooth_id` .
It specifies which smoothing to apply at this node.

.. meta::
   :keywords: example

.. index:: example

.. _nslist_pair_table@Example:

Example
*******
The file :ref:`nslist_pair_table.py-name`
contains an example ``nslist_pair`` table.

.. toctree::
   :maxdepth: 1
   :hidden:

   nslist_pair_table.py
